[tool.coverage.run]
branch = true
source = ["aiomongo"]
omit = ["tests/*"]

[tool.coverage.paths]
source = ["src", ".tox/*/site-packages"]

[tool.coverage.report]
show_missing = true

[tool.black]
exclude = '''(
setup\.py
)
'''